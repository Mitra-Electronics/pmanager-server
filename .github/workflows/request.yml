name: Lint with mypy

on: 
    schedule:
    - cron: "*/5 * * * *"

jobs:
  mypy:
    runs-on: ubuntu-latest
    steps:
      - name: Request
        uses: satak/webrequest-action@master
        with:
            url: https://mstudents.onrender.com/
            method: GET